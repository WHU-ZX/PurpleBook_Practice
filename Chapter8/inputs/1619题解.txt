显然，枚举最小值，再求出每个最小值所对应的最大的区间 比 枚举区间，求出区间中的最小值容易

首先应该用L[i]表示第i个数的区间左部的下标，R[i]表示第i个数的区间右部的下标，初始时L[i]=R[i]=i，然后
for (int i = 1; i <= n; ++i)
{
	while (numbers[i] <= numbers[L[i] - 1]) L[i] = L[L[i] - 1];
}
for (int i = n; i >= 1; --i)
{
	while (numbers[i] <= numbers[R[i] + 1]) R[i] = R[R[i] + 1];
}

针对第一个循环，当一个数比他当前区间左部左边的数还小，证明它的区间左部能延申到它区间左部左边的数的区间左部，直到不能延申为止，这样求比每次从i向左移动直到遇到比numbers[i]大的数停止这种做法是要快一些的，之前我就每次从i向左移，结果时间不够了